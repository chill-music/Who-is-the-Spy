# ⛔ LEGACY CODE - DO NOT USE
The files in this directory (`src/core_LEGACY/` and `src/utils/auth_LEGACY.js`) are unused, legacy copies of code that have been replaced by more modern modular versions (e.g., `01-config.js`, `ProfileHelpers.js`).

They have been moved here during the **PRO SPY Optimization Phase 1** to prevent component collisions and silent overrides. 

**Do NOT import from these files.** They are kept only for reference/revert purposes.

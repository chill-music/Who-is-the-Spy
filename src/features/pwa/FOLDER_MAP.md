# src/features/pwa Directory Map

Manages Progressive Web App (PWA) specific logic, including caching and update lifecycle.

## Core Logic

| File | Purpose |
| :--- | :--- |
| **`PWAHandler.js`** | Orchestrates the `BeforeInstallPrompt` and `ServiceWorker` lifecycle events for better UX. |
